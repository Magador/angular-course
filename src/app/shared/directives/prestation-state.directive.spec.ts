import { PrestationStateDirective } from './prestation-state.directive';

describe('PrestationStateDirective', () => {
  it('should create an instance', () => {
    const directive = new PrestationStateDirective();
    expect(directive).toBeTruthy();
  });
});
