import { ClientStateDirective } from './client-state.directive';

describe('ClientStateDirective', () => {
  it('should create an instance', () => {
    const directive = new ClientStateDirective();
    expect(directive).toBeTruthy();
  });
});
